

# config
config_dict = {"input shape": (169, 35, 12),
               "units": 20,
               "dropout": 0.5,
               "batch size": 8,
               "epochs": 50,
               "learning rate": 0.005,
               "reduce learning rate": 0,
               "lr scheduler": 1}

